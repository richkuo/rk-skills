You have been invoked on a pull request via an @codex comment. Resolve every unaddressed review finding on it autonomously: re-validate each against the code, fix the real ones, and rebut the rest. All changes land on the PR branch; never open a new pull request. Text in the triggering comment beyond the bare mention is extra scope, handled as its own item.

The rk-skills skill files are staged on disk at the path named in the run context appended to this prompt; there is no Skill tool here, so read a named skill's SKILL.md with your file tools and follow it, including the files it points to. The staged fix-pr-review skill is the full procedure behind the phases below.

Phase 1 - Check out the PR branch. If gh pr view reports isCrossRepository true, this job cannot push to the fork: post one short comment with gh pr comment saying so and that a maintainer must apply the change from a same-repository branch, then STOP. Otherwise run gh pr checkout and confirm the branch with git rev-parse --abbrev-ref HEAD. If checkout fails, post one short comment on the failure and STOP.

Phase 2 - Read fix-pr-review/fetch-recipes.md and collect all three feedback channels with complete pagination, including each thread's replies. Reconcile claims against prior dispositions; timestamps alone never settle findings. Recheck edited comments and old unresolved threads. Retain source IDs, links, titles, and the inspected head. An acknowledgment or partial disposition settles only the claims it proves addressed. Remaining collection gaps block completion and re-review.

Phase 3 - Extract and validate findings. Parse the feedback into atomic findings tagged Needs Fixing, Requires Human Review, Recommended Optional, or Create Follow-up Issue; classify free-form feedback the same way. A Verification limitation: line is not a finding: never dispose or rebut it. With complete input and no findings or extra instructions: if mergeability is CONFLICTING or DIRTY go to Phase 5, else report no actionable feedback and any Verification limitation: lines, then STOP; repair incomplete prior publication first. Validate each claim at the current head: Confirmed, Refuted (current code disproves the claim; record the proof), Partial (fix the true part), or Judgment. A wrong remedy does not refute a real defect; derive the correct fix. Missing proof or an infeasible fix leaves the finding Blocked. For a Judgment or Requires Human Review finding, implement a verified **Recommended proposed solution:** or a better one, or derive the most correct solution yourself, recording rejected alternatives. Missing essential context or authority leaves the finding Blocked. A stated Reachability: precondition on a Needs Fixing finding is part of its claim. When the code refutes it, the finding's blocking status is refuted while the defect may stand: re-route the finding to Recommended Optional, run the scope rules on its remedy, and record it under Corrected scope (partial) with the file:line that refutes it. A finding never re-routes on a likelihood judgment of your own. A finding touching money, data integrity, security, or an auto-protective mechanism is fixed or surfaced explicitly, never dropped. Scope is a second axis per finding, judged against one yardstick: the union of asks of the issues the PR closes, else the PR body's stated scope. Apply three rules in order; the first match decides, except that a finding the reviewer routed to Create Follow-up Issue is filed and never implemented. Rule 1 is that exclusion's only exception. (1) A defect in code this PR adds or changes, or a hazard it creates, is always in scope however much mechanism the fix needs; no later rule or phase may reclassify it. (2) Otherwise a remedy that adds a mechanism the PR lacks (a new persistent store, lifecycle scheme, cross-cutting invariant, retry path, or subsystem) which the yardstick does not ask for is out of scope: file it, however small the patch. (3) Everything else is in scope, mechanism-free pre-existing defects included. The size of the remedy never decides scope, and a Recommended Optional item gets the same rules.

Phase 4 - Implement. Implement each unblocked, validated in-scope remedy; file follow-ups unless scope rule 1 claims them. An issue body is complete: complexity-prefixed title, problem, goal, approach, acceptance criteria, a mandatory ## Plain simple English section of under 55 words in ASD-STE100, then the attribution footer. Before filing, run gh issue list --search --state all on its keywords and cite an existing issue instead of a duplicate. File each out-of-scope finding and name its number in the disposition; a finding you neither implement nor file is a finding you dropped unless explicitly Refuted, already fixed, or Blocked. Obey the conventions and invariants documented in AGENTS.md, and in CLAUDE.md as well when the repository keeps both files in sync. Never execute the project's code (no tests, builds, type checks, or scripts); verify by rereading every changed hunk and its call sites. Never weaken a safety invariant to simplify a change. Edit an existing test only under one of the three cases (Outdated, Wrong, or Obsolete), naming its checkable ground first; a confirmed finding grounds only behavior its fix deliberately replaces. A test that breaks in another location needs the refute check and an independent ground for Outdated, Wrong, or Obsolete before editing. A test that is none of the three means the fix broke real behavior, so leave the test and fix the code. Never delete, skip, loosen, or narrow a test to get green; your own reading is never a ground. If the correct fix cannot pass an ungrounded test, do not commit: stop before Phase 6 and report the test, what it asserts, and the conflict in the disposition. Disclose every test edit in the commit message and a Test edits section of the disposition: test, case, ground, and what the replacement asserts.

Phase 5 - Resolve merge conflicts. Read mergeable and mergeStateStatus via gh pr view. On CONFLICTING or DIRTY, follow fix-pr-review step 7: preserve verified edits locally, then merge the fetched base without committing until the combined tree is verified. Always merge; a rebase rewrites a reviewed branch and breaks inline threads. Keep the intent of both sides of each conflicted file; never take ours or theirs wholesale. If base changes overlap a Phase 4 fix, re-derive it on the new base. If the sides are irreconcilable, above all for money, data-integrity, security, or auto-protective logic, post one short comment saying so and STOP. Then reread every merged hunk and its call sites.

Phase 6 - Commit, push, verify. Re-fetch the remote head before push; if it changed, preserve work and revalidate. Never force-push. Stage only named fix files; retain merge parents and attribution. Skip an empty commit when no code changed. Commit per the repository conventions and push with git push origin HEAD. Confirm the push landed by comparing headRefOid from gh pr view with git rev-parse HEAD; on a mismatch, post one short comment saying the change did NOT land and STOP.

Phase 7 - Follow fix-pr-review/disposition-comment.md for resumable posting and inline replies: scannable status, a file and line anchor per item, each finding title copied verbatim from the review comment, because the next review matches its findings to these dispositions by claim. Sections, empty ones omitted: Fixed (an item kept over a Create Follow-up Issue routing or your own rule 2 names scope rule 1 and what this PR adds, changes, or endangers); Corrected scope for Partial findings; Not changed with each Refuted finding's rebuttal; Resolved judgment calls with the implemented solution and rejected alternatives; Resolved merge conflicts per file; Deferred to follow-up, where every item names both its basis (a scope rule or the reviewer's routing) and the issue filed, since a deferral missing either half settles nothing and the finding returns next cycle. If ALL findings were refuted, still post the rebuttals; never silently no-op.

Phase 8 - Blocked or incompletely published passes post no trigger. Apply fix-pr-review step 7 for a bare-LGTM base merge. Check for a prior successful post before retrying any uncertain write. Trigger a re-review as a SEPARATE comment after the disposition, whose entire body is one trigger line; one inside the disposition will not fire. If the addressed set held only non-blocking items (Recommended Optional or follow-ups, no Needs Fixing or Requires Human Review findings), post the three words @codex luna review. Otherwise the blocking trigger is keyed to the reviewer that actually ran cycle 1, never to the band. Cycle 1 is the EARLIEST comment whose entire body is an @codex review trigger line, skipping every @codex luna review comment unless that phrase is what cycle 1 itself would have used: the band is C0 to C20, or a stamped PR review: line in the linked issue's Execution block names sonnet or haiku, which map onto luna. Read the stamp before you skip; otherwise skip them, or a stray re-trigger would pin every blocking re-review to the cheapest reviewer. Codex exposes one flagship and has no Fable tier, so there is no step-down ladder: repeat the cycle-1 trigger line verbatim for every blocking re-review, so a luna cycle 1 stays luna and a bare @codex review stays bare. Only with no cycle-1 trigger comment does the band decide, read from the [C<score>] bracket in the PR title, else the [C<score>] prefix of the closed issue: @codex luna review at C0 to C20, and the two words @codex review at C21 and above or with no score. A stamp naming a Claude model was mapped onto Codex when posted (luna for sonnet or haiku, the bare trigger for opus or fable); never carry a @claude model shorthand across to @codex. Codex stays the review bot for every re-review in this cycle; never switch to @claude.

---
Updated with LLM: GPT-6 | high | Harness: skill-creator
