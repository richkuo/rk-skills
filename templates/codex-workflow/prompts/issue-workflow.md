You have been invoked on a GitHub issue by an @codex comment. Run three phases in order, and do not skip phase 1 even if the issue looks obviously correct.

The rk-skills skill files for this run are staged on disk at the absolute path named in the run context appended to this prompt. There is no Skill tool here: read the SKILL.md file of the named skill with your file tools and follow its steps literally, including the files it tells you to read next.

Phase 1 — Validate. Follow the staged validate-issue skill on this issue. Verify every factual claim in the issue body against the actual code with file:line citations, and judge whether the proposal is architecturally feasible and self-consistent, before doing anything else.

Phase 2 — Correct. If validation finds claims that are inaccurate, out of date, missing critical detail, or contradicted by the code, edit the issue body with the gh CLI (gh issue edit) to fix it, and post one short comment summarizing exactly what you changed and why. The corrected body must still satisfy the staged github-issue-format contract, including the mandatory ## Plain simple English section of under 55 words in ASD-STE100 after the acceptance criteria — add that section when the body has none, and rewrite it when the corrected problem no longer matches it. If the issue describes something already implemented, fundamentally contradicted by the code, or not feasible as written, post the validation findings as a comment and STOP — do not write any code.

Phase 3 — Implement. Only if the issue is valid and actionable, implement it. Follow the staged work-on-issue skill on this issue — it owns the implement, verify, commit, push, and pull-request flow. Take its Cursor or Codex path: there is no EnterWorktree tool here, so create the worktree with git worktree add under .claude/worktrees/codex/issue-N-slug on a branch named codex/issue-N-slug. While following it, obey the conventions and invariants documented in AGENTS.md, and in CLAUDE.md as well when the repository keeps both files in sync. Never execute the project's code in this workflow: do NOT run test suites, builds, type checks, simulations, or scripts. Reason carefully about correctness and runtime behavior by rereading the changed code and its call sites; if the repository has CI, it runs checks on the pull request separately. Never weaken a money, data-integrity, security, or privacy invariant to make a change simpler; correctness and safety outrank a smaller diff.

This run was started by an @codex comment, so Codex is the selected review bot for this cycle. Where the staged skills would post an @claude review trigger, post the @codex form instead: request the first review on the pull request you open with a separate comment whose entire body is one trigger line, routed by the issue's complexity band read from its [C<score>] title prefix: the three words @codex luna review at C0 to C10, and the two words @codex review at C11 and above or when the issue carries no score. Codex exposes one flagship, so every band above C10 collapses onto that bare trigger. Every re-review in this cycle stays on @codex.

Write any issue or PR comment concisely, as direct instructions an agent can read and act on, with no preamble or filler.
