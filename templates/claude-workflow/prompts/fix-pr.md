You have been invoked on a pull request via an @claude comment requesting changes (for example, addressing review feedback). Make the changes in place on the pull request's own branch and push to it — never open a new pull request.

Phase 1 — Check out the PR branch, refusing fork-based PRs. First determine whether the pull request comes from a fork: run gh pr view on it reading the JSON field isCrossRepository. If that reports true, the PR head branch lives on the contributor's fork, which this job cannot push to — it can only push to origin, the base repository — so an in-place fix is impossible: post one short comment with gh pr comment stating that a fork-based PR cannot be fixed in place and that a maintainer must apply the change from a same-repository branch, then STOP. Otherwise run gh pr checkout on the pull request so your working tree is on the PR head branch, and confirm with git status and git rev-parse --abbrev-ref HEAD before making any edits. If checkout fails, post one short comment with gh pr comment explaining the failure and STOP.

Phase 2 — Understand the request. Read the triggering comment. When it refers to review feedback, read the latest review and the diff with gh pr view and gh pr diff to see exactly what must change. If the request is ambiguous, self-contradictory, already satisfied, or outside the scope of this pull request, post one short comment with gh pr comment explaining what you need or what you found and STOP — do not guess.

Phase 3 — Implement. Make the smallest correct change that satisfies the request. Obey the conventions and invariants documented in CLAUDE.md. Never execute the project's code in this workflow: do NOT run test suites, builds, type checks, simulations, or scripts. Verify by rereading every changed hunk and its call sites; if the repository has CI, it runs checks on the pull request separately. Never weaken a money, data-integrity, security, or privacy invariant to make a change simpler; correctness and safety outrank a smaller diff.

Phase 3.5 — Resolve merge conflicts with the base branch. Check the pull request's mergeability via gh pr view reading the JSON fields mergeable and mergeStateStatus. If it reports CONFLICTING or DIRTY, resolve the conflicts on the head branch, never on the base: run git fetch origin followed by git merge origin/<baseRefName> — always merge, never rebase, because the branch is already pushed and reviewed. Resolve each conflicted file by reading both sides and preserving the intent of both changes — never mechanically take ours or theirs for a whole file; if base-branch changes overlap code you just changed, re-derive your change on top of the new base code. If the two sides are genuinely irreconcilable in intent — especially for money, data-integrity, security, or auto-protective logic — do not guess: post one short comment with gh pr comment explaining the irreconcilable conflict and STOP. Reread every merged hunk and its call sites, and mention the conflict resolution in your summary comment.

Phase 4 — Commit, push, and verify the push landed. Stage your changes, commit following the repository commit conventions, and push to the PR branch with git push origin HEAD. Then confirm the push actually updated the pull request: read the PR head commit via gh pr view reading the JSON field headRefOid, and compare it to your local HEAD from git rev-parse HEAD. If they do not match, the push did not reach the PR — post one short comment with gh pr comment reporting that the change did NOT land on the pull request and STOP without claiming success. When they match, post one short comment with gh pr comment summarizing exactly what you changed, written as direct instructions an agent can read and act on, with no preamble or filler. If you determined no change was needed, post a comment saying so and why instead.
