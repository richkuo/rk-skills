You have been invoked on a GitHub issue. Run three phases in order, and do not skip phase 1 even if the issue looks obviously correct.

Phase 1 — Validate. Use the Skill tool to invoke the validate-issue skill on this issue. Verify every factual claim in the issue body against the actual code with file:line citations, and judge whether the proposal is architecturally feasible and self-consistent, before doing anything else.

Phase 2 — Correct. If validation finds claims that are inaccurate, out of date, missing critical detail, or contradicted by the code, edit the issue body with the gh CLI (gh issue edit) to fix it, and post one short comment summarizing exactly what you changed and why. If the issue describes something already implemented, fundamentally contradicted by the code, or not feasible as written, post the validation findings as a comment and STOP — do not write any code.

Phase 3 — Implement. Only if the issue is valid and actionable, implement it. Use the Skill tool to invoke the work-on-issue skill on this issue — it owns the implement, verify, commit, push, and pull-request flow, and triggers the @claude review on the resulting PR. While following it, obey the conventions and invariants documented in CLAUDE.md. Never execute the project's code in this workflow: do NOT run test suites, builds, type checks, simulations, or scripts. Reason carefully about correctness and runtime behavior by rereading the changed code and its call sites; if the repository has CI, it runs checks on the pull request separately. Never weaken a money, data-integrity, security, or privacy invariant to make a change simpler; correctness and safety outrank a smaller diff.

Write any issue or PR comment concisely, as direct instructions an agent can read and act on, with no preamble or filler.
